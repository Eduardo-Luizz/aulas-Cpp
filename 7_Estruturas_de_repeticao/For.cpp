#include <iostream>
using namespace std;
#include <stdlib.h>

int main()
{
    system(" cls ");

    int num = 0;

    // São os tres parametros dentro do for
    // inicializar a variavel, posso declarar apenas la dentro
    //Atualizar a variavel
    //Teste de repeticao
    

    //Não necessariamente preciso ter o num, posso inicializar uma variavel para o controle do laco
    //Posso colocar dentro do for mas nao preciso expecificar os tres parametros iniciais
    for (num = 0; num <= 100; num += 10){
        cout << num << endl;
    }
}